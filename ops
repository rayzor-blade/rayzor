{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789849538,"nonce":"XCvDMhDyNIQOPFP2AghsAGYO+ZY=","title":"a parameter incremented with `++p` in a loop body was not loop-carried","message":"`find_modified_variables_in_expression` recursed into a unary operand but never recorded the operand of `++`/`--` as written. A local has a `locals` entry and is carried anyway; a PARAMETER is carried only when the body assigns it, so `while (p \u003c n) { ...; ++p; }` left `p` as the signature register: the exit read a value defined in the body (`Left operand IrId not found`) and the loop never advanced. `p += 1` was fine.","files":null}]}