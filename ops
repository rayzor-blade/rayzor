{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789949666,"nonce":"IOLlkuglSY8y3MgtvHq/YQPd0vs=","title":"A method declared below its caller with an inferred return type decays to Dynamic at the call","message":"static function main() { if (cat(\"x\") != \"ax\") throw \"...\"; }   // throws\n    static function cat(s) return \"a\" + s;\n\nThe caller is lowered before the callee's body, so the callee's symbol\nstill carries no return type; the call is typed Dynamic and `!= \"ax\"`\ncompares a raw String pointer as a box. 2d7c200a recovered only\n`new C\u003cT\u003e()`.\n\nTried 2026-09-21 and reverted: lowering method bodies callee-first\n(DFS over same-class calls, declaration order kept for the emitted list).\nIt types `haxe.Template.makeExpr` as `Void-\u003eDynamic`, which boxes the\nclosure at `var e1:Dynamic = makeExpr(l)`, and `::if (n \u003e 2)::` then took\nneither branch. The function-unbox at Let and return (665d146e) is the\nfirst half of what that needs; retry after the Dynamic call-site work.","files":null}]}