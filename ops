{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789934542,"nonce":"0w1v6Z1jHgoKdaYXxVMM889WSMw=","title":"`if (q)` / `!q` / `q \u0026\u0026 r` on a `?q:Bool` parameter read the box pointer","message":"Since `?x:Bool` is `Null\u003cBool\u003e` (eb68d0ec) a boxed Bool reached every truth\nsite raw: `f(false)` took the true branch, `!q` printed the pointer, the LLVM\ntier rejected the program (\"branch condition is a reference\"). TestXML's\nattribute quoting depended on the pointer's low bit and flipped between\nbuilds. Fix: `truth_of` opens a `Null\u003cBool\u003e` register with\n`haxe_unbox_bool_ptr` (null reads false) at if/while/ternary conditions,\nswitch guards, `!` and both operands of `\u0026\u0026`/`||`. Fixture NullBoolTruth.hx.","files":null}]}