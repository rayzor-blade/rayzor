{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789117862,"nonce":"xY1sbvPzC+tIMxnJ4P9O1p+eD6Y=","title":"haxe.rtti.Meta.getFields loses a field's own annotation","message":"`haxe-conformance/regressions/RuntimeMetadata.hx` fails with\n`Uncaught exception: field metadata`, i.e.\n\n    var fields = haxe.rtti.Meta.getFields(Annotated);\n    if (fields.value.fieldTag[0] != \"field\") throw \"field metadata\";\n\nThe neighbouring reads in the same fixture all succeed, so the defect is\nspecific to per-FIELD metadata rather than to metadata as a whole:\n\n- `Meta.getType(Annotated).answer[0] == 42`      OK  (type, Int arg)\n- `Reflect.hasField(metadata, \"tag\")`            OK  (marker annotation)\n- `metadata.nested[0].text == \"hello\"`           OK  (nested anon + String)\n- `Meta.getFields(Annotated).value.fieldTag[0]`  FAILS\n\nMEASURED PRE-EXISTING at 32018b5d: built HEAD with the working tree stashed and\nre-ran `check-regressions.py` -- the same five fixtures pass and this one fails,\nso it is not a regression from the Dynamic-representation work in flight.\n\nThe fixture arrived with d71afd94 (\"feat: abstract operators, runtime metadata,\nand named native crashes\"); the other five fixtures from that area pass.\nNot covered by the conformance corpus -- the only signal is this fixture, which\n`haxe-conformance/run.sh` does NOT run. Run it with\n\n    RAYZOR=\"$PWD/target/release/rayzor\" python3 haxe-conformance/check-regressions.py","files":null}]}