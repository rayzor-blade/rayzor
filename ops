{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789854710,"nonce":"gpPlZr92EAttOOQXAyCXLGChQhg=","title":"reading past an array's length faulted instead of yielding null","message":"`var a:Array\u003cXml\u003e = []; a[0]` dereferenced the empty array's null data pointer (`Xml.firstChild()` on a childless element, `children[0]`). Haxe reads out of bounds as null. The inline element address stays unchecked-branchless for vectorization: the index and base are clamped by `select` onto the array struct itself (valid memory) and the loaded value is replaced by zero/null when the unsigned index is not below `len`. Watch the CI benchmarks: this adds a length load, a compare and three selects per array read.","files":null}]}