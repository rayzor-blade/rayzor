{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789958726,"nonce":"/YDz0l68PabmxTp7r06Jrs+zH7E=","title":"A scalar written into a generic class's `Null\u003cT\u003e` field was stored raw; `++` on a `Null\u003cInt\u003e` incremented the box address; a null nested enum payload was dereferenced","message":"Found with CR1-CR6 probes (scratchpad/probe), from the Issue2281/Issue2580\ncrash rows.\n\n1. `Foo\u003cT\u003e.valNull:Null\u003cT\u003e`: the field slot is a box (`convert_type` gives\n   `Null\u003cT\u003e` the box pointer type) but `f.valNull = 0` stored the raw 0 —\n   `maybe_box_value` only boxes for `Null\u003cscalar\u003e`, and T is not a scalar.\n   The read then saw a null box. A `Null\u003cT\u003e` target now boxes a scalar\n   register too (`optional_inner_is_type_param`).\n\n2. `n++`, `++n`, `n--` on any `Null\u003cInt\u003e` (local, field or element) read the\n   box pointer, added one to the ADDRESS and stored that: `n 0 41047788769`.\n   Pre-existing on 6113ac5e as well. The three increment paths open the box\n   (`open_nullable_scalar`), count on the scalar and box the result; a\n   post-increment yields the original box, a pre-increment the new one.\n\n3. `case T1(T1(_))` against `T1(null)` loaded the nested payload's tag from\n   address 0. The boxed-constructor test now branches on null first\n   (`lower_boxed_constructor_test` runs only for a non-null value).\n\n4. A `Null\u003cT\u003e` box over an unbound T (`new Foo()` with T never inferred),\n   and a known box the typer called a bare scalar (the result of `n++`),\n   handed to an imported callee's erased formal passed the box ADDRESS;\n   `unbox_optional_args_for_erased_formals` now hands over the payload\n   (`haxe_dynamic_to_slot`, or the typed unbox).\n\nAlso: a switch subject matched against constructor patterns types an\nunannotated parameter as that enum (`function match(t) switch (t) { case\nT1(..): }`).\n\nFixture: haxe-conformance/regressions/NullableFieldIncrements.hx.","files":null}]}