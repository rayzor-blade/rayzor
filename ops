{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789849539,"nonce":"4xJ+7+uUvW/2ElTDhWA4wabfgo4=","title":"for-in over a class whose iterator() returns Iterator\u003cT\u003e emitted no loop, and typed the element Dynamic","message":"The compiled-iterator() path looked for a CLASS in the method's return type to find hasNext/next; `Iterator\u003cT\u003e` is a typedef over a structure, so it fell to a scan of every class with hasNext+next, then `return` -- zero iterations, silently (haxe.xml.Printer printed `\u003cfoo/\u003e` for every element). Such a return is a handle, so the loop is the handle loop now. The typer also left the loop variable Dynamic because `structural_element_type` only read a typedef's structure and never a class's own `iterator()` declaration; `child.kind` then lowered as a dynamic field read and faulted. Still open: an UNANNOTATED `function it() return items.iterator()` types as the concrete ArrayIterator class and the loop yields nothing.","files":null}]}