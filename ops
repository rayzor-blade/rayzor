{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789961128,"nonce":"F9ZYnk1qQJOF/YVEUESGQ128yhU=","title":"`@:from` skipped at field initializers and field assignments; a typedef of a generic abstract lost its arguments; a null element in an array pattern fell to a legacy layout","message":"From the Issue3244 / Issue3127 / Issue3031 crash rows (probes FR1, VT1-3, AP1-3).\n\n1. `static var a:A = 1`, `var b:A = 2`, `c = 3` in a constructor, with\n   `abstract A(String) from String { @:from static function fromInt ... }`:\n   the Int was stored raw. A literal initializer of an abstract-typed static\n   was folded as a constant (metadata.rs); the constructor's field\n   initializers and field-target assignments applied no conversion. All\n   three now run `maybe_abstract_from_convert`, as a local and an argument\n   already did.\n\n2. `typedef V\u003cT\u003e = haxe.ds.Vector\u003cT\u003e; new V\u003cInt\u003e(3)` took the abstract's\n   value-wrap path (the Vector WAS the Int 3, reading address 0xb), and\n   `new VI(3)` for a non-generic alias the same. Type lowering followed a\n   typedef with substituted arguments only for Array and EXTERN targets; it\n   now follows any abstract target. The abstract constructor call, the\n   `@:arrayAccess`/`@:op` finders, `resolve_type_param_from_receiver` and\n   the ArrayAccess result typing see through a typedef too.\n\n3. `switch [x1, x2] { case [null, null]: ... case [a, null]: a.s ... }`: the\n   array-pattern desugaring handled Var, `_` and constants and fell back to\n   the MIR Array pattern for `null`, which assumes a `{length, elems..}`\n   layout HaxeArray never had; the function then failed to lower (\"Return\n   with no value\"). A `null` element compares the indexed element with null.\n\nFixture: haxe-conformance/regressions/AbstractFromAndAliases.hx.","files":null}]}