{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1790374202,"nonce":"w6XXbGOBuNL188SNkUNr2kiOQc8=","title":"Generic inference gaps, constructor values, bare-return lambdas and BOM sources","message":"Rows blocked in report-bw (682), each reproduced as a probe:\n\n- Generic trap stubs (`unit.Test.aeq`/`eq` never specialized):\n  - a function literal's own type parameters (`function rest\u003cT\u003e(..)`) were never registered, so `T` in its signature resolved to something concrete and the caller's inference hit a conflict;\n  - a call through a local generic function kept the callee's `T` in its result type (return types were substituted for StaticMethodCall only);\n  - a variable met only by Dynamic arguments (`aeq((..:Array\u003cDynamic\u003e), restDynamic(..))`) bound nothing; Haxe binds it to Dynamic;\n  - a method's pre-registered signature was lowered without its own type parameters in scope; they are now scoped. This did NOT fix Issue9046 (see below).\n- `C.new` as a value (Issue6752, 3333, 9764; also plain classes): no function value existed. Now lowered as `function(a..) return new C(a..)` typed from the constructor.\n- Abstract constructor `new(i) this = i`: `i` stayed Dynamic, so a call through the value boxed it. `this = p` now types `p` as the underlying type.\n- Lambdas with a bare `return;` whose tail had a value (Issue7376): MIR inferred the return type from the tail register and Cranelift rejected the value-less return.\n- `[5, 6.2, 7]` was Array\u003cInt\u003e (first element's type); Float elements were stored as boxes. Int elements now widen.\n- TestBytes/TestIO/TestResource/TestSerialize begin with a UTF-8 BOM; the lexer did not skip it (`Cannot find name 'eq'`).\n\nResult: report-bx 686 (from 682), 0 LOST; GAINED Issue10030, Issue3333, Issue6752, Issue7376. MOVED Issue9764 CRASH -\u003e WRONG_ANSWER; TestIO/TestResource now get past the BOM to later failures. Suite 206/0/2, fixtures green, compiler integration tests green. Fixture: haxe-conformance/regressions/ConstructorValuesAndLocalGenerics.hx.\n\nStill open after this batch:\n- Issue9046: ORDER-DEPENDENT. `var a = U.flatten('hello'); aeq(['hello'], a);` inside a subclass leaves inherited `aeq` unspecialized when class U is declared BELOW the caller; passes when U is declared first (with or without @:generic). The pre-registration scoping above did not change it -- needs instrumentation of the argument types at the aeq call.\n- Issue9764: `new(a) this = new Base(a)` leaves `a` Dynamic (needs constructor-argument inference).\n- A local generic with rest args (`rest\u003cT\u003e(...values:T)` called with Int and Float) reads a boxed element as raw.\n- Issue2778 (GADT), Issue3970 lambda, Issue10754 (`a.push.bind()`), Issue10124 test1 crash.","files":null}]}