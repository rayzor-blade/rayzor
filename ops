{"author":{"id":"8022379585b4ab99ca8775db3eb67d33fcb256817114c28e51ccc1e9f55cdec4"},"ops":[{"type":1,"timestamp":1789900758,"nonce":"Tu6XkTs+xQdQzJ89apkC5xQ425s=","title":"the blade cache is shared by concurrent compiles and a reader can see a half-written module","message":"run.sh runs the corpus with JOBS in parallel from the repo root, all writing `.rayzor/blade/cache`; a `run_haxe_tests.sh` started right after it saw `Called function IrFunctionId(360003) not found in module` for `MyException.new` -\u003e `haxe.Exception.new` (test_exception) three gates in a row, and the same test passes alone a minute later with the same binary. The cache is keyed by build id, so this is a concurrent-write/partial-read race (or a module written by a compile with a different import numbering), not staleness. Write to a temp file and rename, and key the artifact by the import set it was compiled in.","files":null}]}